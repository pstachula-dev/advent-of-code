import { runner } from "../../utils";

// P1
const part1 = (data: number[]) => {
};

runner((input) => {
  const data = input.map((e) => parseInt(e));

  return part1(data);
}, "./2021/DAY_NUM/input.txt", 'time p1');

const part2 = (data: number[]) => {
};

runner((input) => {
  const data = input.map(e => parseInt(e));

  return part2(data);
}, "./2021/DAY_NUM/input.txt", 'time p2');
